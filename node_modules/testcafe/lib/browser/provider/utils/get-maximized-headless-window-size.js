'use strict';

exports.__esModule = true;

exports.default = function () {
    const sizeString = process.env.MAXIMIZED_HEADLESS_WINDOW_SIZE || DEFAULT_MAXIMIZED_HEADLESS_WINDOW_SIZE;

    var _sizeString$split$map = sizeString.split('x').map(str => Number(str));

    const width = _sizeString$split$map[0],
          height = _sizeString$split$map[1];


    return { width, height };
};

const DEFAULT_MAXIMIZED_HEADLESS_WINDOW_SIZE = '1920x1080';

module.exports = exports['default'];